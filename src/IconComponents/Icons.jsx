const Icon = (iconName) => {
    let iconPath;
    switch (iconName) {
        case "value":
            
            break;

        default:
            break;
    }
    return (
        <img src={iconName} alt="" />
    )
}

export default Icon